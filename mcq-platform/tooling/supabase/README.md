# Supabase Bootstrap

- ootstrap_notes.sql: creates the public.notes table, seeds sample rows, enables RLS and applies the public can read countries policy.
- Run the script via the Supabase SQL Editor or psql before testing the Next.js /notes route.
